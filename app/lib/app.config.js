const CONFIG = {
  environment: 'PRODUCTION', // DEMO, PRODUCTION, DEVELOPMENT
  version: '0.0.1'
};

export const ENVIRONMENT = CONFIG.environment;
export const VERSION = CONFIG.version;

export default CONFIG;
